version: "3.1"
services:
  glm_test:
    image: jrossusgs/glm3r:v0.6a
    build:
      context: .
    ports:
      - "8787:8787"
    environment:
      - ROOT=TRUE
      - PASSWORD=mypass
    volumes:
      - "../res-temperature-process-models:/home/rstudio"

